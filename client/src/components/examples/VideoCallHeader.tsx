import { VideoCallHeader } from '../VideoCallHeader';

export default function VideoCallHeaderExample() {
  return (
    <div className="relative w-full h-32 bg-gradient-to-br from-blue-900 to-purple-900">
      <VideoCallHeader />
    </div>
  );
}
